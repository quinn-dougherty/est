x = Manifold.est(creator="Austin", market_slug="")
y = Manifold.est(creator="Quinn", market_slug="")

Lib.aggregate((x + y) / 2) // should evaluate, with confidence a function of a given liquidity measurements.
